language: java
jdk: openjdk8

branches:
  only:
    - master
    
build:
    ci:

    - RUN mkdir -p shippable/testresults
    - RUN mkdir -p shippable/codecoverage
    - shippable_retry mvn install -DskipTests=true
    - cp -r target shippable/codecoverage